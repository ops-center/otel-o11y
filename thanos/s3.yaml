type: S3
config:
  bucket: "test"
  endpoint: "minio.minio.svc.cluster.local:443"
  access_key: "minio"
  secret_key: "minio123"
  # insecure: true
  http_config:
    insecure_skip_verify: true
prefix: "metrics"
