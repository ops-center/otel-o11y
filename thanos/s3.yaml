type: S3
config:
  bucket: "test"
  endpoint: "minio.minio.svc.cluster.local:9000"
  access_key: "rootuser"
  secret_key: "rootpass123"
  # insecure: true
#  http_config:
#    insecure_skip_verify: true
prefix: "metrics"
